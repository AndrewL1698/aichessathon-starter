# nnue h256-100m-e71 — `weights/nnue-h256-100m-e71.npz` (absolute target, epoch 71, final)

The patience stop of the 163M-position run ([h256-100m](h256-100m.md) is epoch 36 of the same
run, exported earlier under deadline pressure; [h256-100m-e11](h256-100m-e11.md) is the interim).
Same data, settings and pipeline commit (582dd78).

## Training

Stopped by `--patience 10 --min-delta 1e-5` after epoch 71: best val **0.013992 at epoch 71**.
65 s per epoch awake and charging; the wall time was dominated by the machine sleeping and by
six hours in which the process sat SIGSTOPped. **Peak RSS 22.4 GB** (`/usr/bin/time -l`) for
290M rows: 19.1 GB of index rows plus targets, the model, and the batch scratch.

| epoch | train | val |
|---|---|---|
| 11 | 0.014361 | 0.014383 |
| 36 | 0.014058 | 0.014062 |
| 50 | 0.014022 | 0.014060 |
| 60 | 0.013996 | 0.014023 |
| 71 | 0.013979 | **0.013992** |

Mean predictor on this split 0.055424. Against the shipped `nnue-h256-52m-e60` (0.014602 on the
90M split) this is −6.1e-4; against epoch 36 it is −7e-5. Full curve in `h256-100m-history.json`.

Sanity table after epoch 71 (White to move):

```
start +5cp  |  +1 pawn (white) +98cp  |  +1 rook (white) +515cp  |  mate in 1 (white) +637cp
```

The start position's tempo bonus has shrunk to +5 from +27 to +46 in the earlier nets, which is
the number `docs/BENCH_LOG.md` flagged as the side-to-move offset.

## Export

qa=512, qb=1024, qc=128, cp_scale=400, target=cp: headroom 1,055; 296,962 bytes.

- `test_export`: **1.65 cp mean, 6.90 cp p99, 9.44 cp max** (tolerance 5 / 25); re-encode exact
  1000/1000; mirror invariance 1000/1000. OK. The most precise export of any file here.

## Commands

```bash
uv run python -m tools.nnue.export --checkpoint tools/nnue/checkpoints-h256-100m/epoch_071.pt \
  --out weights/nnue-h256-100m-e71.npz --qa 512 --qb 1024
uv run python -m tools.nnue.test_export --weights weights/nnue-h256-100m-e71.npz \
  --checkpoint tools/nnue/checkpoints-h256-100m/epoch_071.pt
```
